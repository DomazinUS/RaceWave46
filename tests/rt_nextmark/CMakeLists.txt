cmake_minimum_required(VERSION 3.20)
project(wr64_rt_nextmark_tests LANGUAGES CXX)
enable_testing()
add_executable(wr64_rt_nextmark_test nextmark_test.cpp)
target_compile_features(wr64_rt_nextmark_test PRIVATE cxx_std_17)
target_include_directories(wr64_rt_nextmark_test PRIVATE ../../lib/rt64/src)
if(MSVC)
    target_compile_options(wr64_rt_nextmark_test PRIVATE /W4)
endif()
add_test(NAME nextmark_classification COMMAND wr64_rt_nextmark_test)
set(WR64_NEXTMARK_ARCHIVE_RAM "" CACHE FILEPATH "Optional original eight-MiB word-swapped native RAM capture")
if(WR64_NEXTMARK_ARCHIVE_RAM)
    add_test(NAME nextmark_native_archive COMMAND wr64_rt_nextmark_test "${WR64_NEXTMARK_ARCHIVE_RAM}")
endif()
